import styled from 'styled-components';

export const ModalContent = styled.div`
  p {
    font-weight: bold;
    color: #444;
    margin-bottom: 1rem;
    font-size: 1.1rem;
    text-transform: uppercase;
  }

  span {
    color: #666;
    text-align: justify;
    font-size: 1.15rem;
    line-height: 1.25rem;
  }
`;
